# Debug 记录：简短标题

## 现象

记录完整报错、运行命令和触发位置。分享前删除本机路径、用户名和其他隐私。

## 最小复现

说明如何用一个样本、一个 batch 或一个模块重现问题。

## 初步假设

写出一个可以被检查的原因，而不是只写“代码有问题”。

## 检查证据

记录输入输出的 `shape`、`dtype`、`device`、数值范围和梯度状态。

## 根因

说明真正不一致的值，以及它们为什么不一致。

## 最小修复

只修改导致问题的部分，不顺手重构无关代码。

## 回归验证

记录最小复现、单 batch 训练和完整评估的结果。
