# PyTorch MNIST 18 决策树和随机森林

> 核心概念：条件分裂、树模型、集成、过拟合

# 第 18 课：树模型和神经网络不是一类东西

决策树和随机森林是机器学习里的另一条路线。它们不像神经网络那样用梯度一步步更新很多权重，而是用一系列条件判断做预测。

你可以先把决策树想成一串问题：

```text
如果特征 A 大于某个值，走左边
否则走右边
继续问下一个问题
最后得到预测结果
```

## 决策树像什么

比如判断一个人今天要不要带伞：

```text
会下雨吗？
风大吗？
要出门多久？
```

决策树就是把这些判断组织成一棵树。每个节点问一个问题，每条分支代表一个答案。

## 它怎么学习

决策树训练时会寻找最有用的问题，把数据分成更容易判断的两部分。

直观理解：

```text
好的分裂：分完之后，每一边都更纯
差的分裂：分完之后，两边还是混乱
```

分类任务里，“更纯”可以理解成同一边的数据更像同一类。

## 随机森林是什么

一棵树容易过拟合。它可能把训练集里的细节记得太死。

随机森林的做法是训练很多棵树，然后让它们一起投票。

```text
树 1 觉得是 7
树 2 觉得是 7
树 3 觉得是 1
最终投票：7
```

很多棵树组合起来，通常比单独一棵树更稳。

## 和神经网络怎么区分

先记住这几个差别：

- 决策树靠条件分裂做判断。
- 随机森林靠多棵树投票。
- 神经网络靠线性层、激活函数、loss 和梯度更新参数。
- PyTorch 更常用于神经网络；树模型常见于 scikit-learn。

现阶段不需要马上掌握 scikit-learn。理解树模型是为了知道：机器学习不只有神经网络。

## 树模型适合什么

树模型经常适合表格数据，比如：

```text
年龄、收入、地区、消费次数 -> 是否购买
面积、楼层、位置、装修 -> 房价
```

图片、语音、文本这类高维数据，深度学习通常更常见。

## Debug 时先查什么

树模型常见问题不是梯度，而是数据本身：

- 训练集和测试集有没有混在一起？
- 标签是不是泄漏到了特征里？
- 类别特征有没有正确处理？
- 训练集表现很好、测试集表现很差，是不是过拟合？

后面做项目复刻时，不管模型多复杂，都要先问：数据有没有被正确拆分？

## 运行脚本

```powershell
python .\mnist_project\18_decision_tree_random_forest.py
```

期待看到：

```text
单棵决策树预测：1
投票明细：[1, 1, 0]
随机森林投票结果：1
决策树和随机森林验证通过
```

这个脚本不依赖 scikit-learn，只用纯 Python 演示“单棵树按规则判断”和“多棵树投票”。它是概念演示，不是完整树模型库。

## 本节小练习

不写代码，先用自己的话回答：

- 决策树是靠什么做判断的？
- 随机森林为什么比单棵树更稳？
- 树模型和神经网络最大的区别是什么？
- 为什么训练集和测试集混在一起会让结果看起来虚高？

## 相关链接

- 索引：[课程索引](../course-index.md)
- 上一课：[第 17 课](17-logistic-regression.md)
- 进度：[学习进度](../progress.md)
- 路线：[十周深度学习路线](../roadmap/deep-learning-plan.md)
